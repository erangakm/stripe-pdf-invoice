-
  date_formatted = moment.unix(date).locale(language).format(date_format);
  due_date_formatted = (due_days && !isNaN(due_days)) ? moment.unix(date).add(due_days, 'day').locale(language).format(date_format) : date_formatted;

  total = (total/100).toFixed(2);
  tax_total_formatted = (tax_total/100).toFixed(2);
  subtotal = (subtotal/100).toFixed(2);
  tax_percent = tax_percent || 0;

  lines.data.forEach(line => {
    line.price = (line.type == 'subscription') ? (line.plan.amount/100).toFixed(2) : (line.amount/100).toFixed(2);
    line.amount = (line.amount/100).toFixed(2);
    if(!line.description && line.type == 'subscription') {
      line.description = ((line.quantity > 1) ? line.quantity + ' * ' : '') + line.plan.name;
      if(line.period) {
        line.period.start = moment.unix(line.period.start).locale(language).format(date_format);
        line.period.end = moment.unix(line.period.end).locale(language).format(date_format);
        line.description += ' ' + line.period.start + ' - ' + line.period.end;
      }
    }
  });
doctype
html.no-js
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    title
    meta(name='description', content='')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    each sheet in css
      link(rel='stylesheet', href=sheet, type='text/css')
    style
      if color
        | .total h1 {
        |   color: #{color};
        | }
      if company_logo
        | .logo {
        |   background-image: url("#{company_logo}");
        |   width: 160px;
        |   background-repeat:  no-repeat;
        |   background-size: 170px;
        |   height: 150px;
        | }
  body.layout
    .row.invoice-body
      .columns
        .row.white-small
          .columns.small-10
            if company_logo
              .row
                .columns.small-5.white-small
                  .logo
            .row
              .columns.invoice
                | #{label_invoice}
          .small-2.columns
            .row
              .columns.date
                | 
        .row.white-small
          .columns.end
            .row
              .columns.component-title
                | #{label_invoice_by}
            .row
              .columns
                | #{company_name}
        .row.white-small
          .small-4.columns
            .row
              .columns.component-title
                | #{label_invoice_to}
            .row.address
              .columns
                .row
                  .columns
                    | #{client_company_address}
                .row
                  .columns
                    | #{client_company_zipcode} #{client_company_city}
                .row
                  .columns
                    | #{client_company_country}
          .small-offset-4.small-4.columns.total
            .row
              .columns.num
              h1
                if currency_position_before
                  | #{currency_symbol}
                | #{total}
                if !currency_position_before
                  | #{currency_symbol}
        if invoice_for
          .row.white-small
            .columns
              .row
                .small-12.columns.component-title
                  | #{label_invoice_for}
              .row
                .small-12.columns
                  | #{invoice_for}
        .row.white-small
          .columns
            .row
              .small-7.columns.component-title #{label_description}
              .small-1.columns.component-title #{label_unit}
              .small-2.columns.component-title #{label_price}
              .small-2.columns.component-title #{label_amount}
              each line in lines.data
                .row
                  .small-7.columns #{line.description || 'Item description'}
                  .small-1.columns #{line.quantity}
                  .small-2.columns
                    if currency_position_before
                      | #{currency_symbol}
                    | #{line.price}
                    if !currency_position_before
                      | #{currency_symbol}
                  .small-2.columns
                    if currency_position_before
                      | #{currency_symbol}
                    | #{line.amount}
                    if !currency_position_before
                      | #{currency_symbol}
        .row
          .small-10.columns #{label_subtotal}
          .small-2.columns.num
            if currency_position_before
              | #{currency_symbol}
            if taxed_transaction
              | #{tax_adjusted_subtotal}
            else
              | #{subtotal}
            if !currency_position_before
              | #{currency_symbol}
        .row
          .small-10.columns #{label_vat}
          .small-2.columns.num #{currency_symbol}#{tax_total_formatted}
        .row.total-line.upper
          .small-10.columns #{label_total}
          .small-2.columns.num
            if currency_position_before
              | #{currency_symbol}
            | #{total}
            if !currency_position_before
              | #{currency_symbol}
    .row.footer
      .columns
        .row
          .small-3.columns
            .row
              .columns.component-title
                | #{label_invoice_by}
            .row
              .columns
                | #{company_name}
            .row.address
              .columns
                .row
                  .columns
                    | #{company_address}
                .row
                  .columns
                    | #{company_state} #{company_zipcode} #{company_city}
                .row
                  .columns
                    | #{company_country}
          .small-3.columns
            .row
              .columns.component-title
                | #{label_invoice_date}
            .row.white-small
              .columns
                | #{date_formatted}
            .row
              .columns.component-title
                | #{label_invoice_number}
            .row
              .columns.num
                | #{id}
          .small-3.columns
            if receipt_number
              .row
                .columns.component-title
                  | #{label_reference_number}
              .row
                .columns.num
                  | #{receipt_number}
          .small-3.columns
            if company_siret
              .row
                .columns.component-title
                  | #{label_company_siret}
              .row.white-small
                .columns.num
                  | #{company_siret}
            if company_vat_number
              .row
                .columns.component-title
                  | #{label_company_vat_number}
              .row
                .columns.num
                  | #{company_vat_number}
